import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function InventoryPage() {
  return (
    <PagePlaceholder
      title="Inventory"
      description="Catalog, partial claims + confirmation, and the stock movements ledger."
      phase="Phase 4"
    />
  );
}
